<h1>Songs#show</h1>

Title: <%= @song.title %>

<%= form_for @song do |f| %>
    <%= f.text_field :title, id:"song_name" %>
    <%= f.text_field :artist_name,  id:"song_artist_name" %>
    <% f.object.notes.each do |note| %>
        <%= note.content %>
    <% end %>
<% end %>
